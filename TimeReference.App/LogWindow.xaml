<Window x:Class="TimeReference.App.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ext="clr-namespace:TimeReference.App.MarkupExtensions"
        Title="{ext:Lang LOGS_TITLE}" Height="450" Width="700" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barre d'outils -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="{ext:Lang LBL_LOG_FILE}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="CmbFiles" Width="180" SelectionChanged="CmbFiles_SelectionChanged"/>
            <Button Content="{ext:Lang BTN_REFRESH}" Width="80" Margin="10,0,5,0" Click="BtnRefresh_Click"/>
            <Button Content="{ext:Lang BTN_DELETE}" Width="80" Margin="5,0,0,0" Click="BtnDelete_Click" Background="#FFDDDD"/>
        </StackPanel>

        <!-- Filtres (Correction : Utilisation de Border pour le Padding) -->
        <Border Grid.Row="1" Background="#F0F0F0" Margin="0,0,0,10" Padding="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{ext:Lang LBL_FILTER}" VerticalAlignment="Center" Margin="5,0,5,0"/>
                <ComboBox x:Name="CmbLevel" Width="100" SelectionChanged="Filter_Changed">
                    <ComboBoxItem Content="{ext:Lang CMB_ALL}" IsSelected="True"/>
                    <ComboBoxItem Content="INFO"/>
                    <ComboBoxItem Content="WARN"/>
                    <ComboBoxItem Content="ERROR"/>
                </ComboBox>
                <TextBlock Text="{ext:Lang LBL_SEARCH}" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <TextBox x:Name="TxtSearch" Width="200" TextChanged="Filter_Changed"/>
            </StackPanel>
        </Border>


        <!-- Grille de données -->
        <DataGrid Grid.Row="2" x:Name="GridLogs" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{ext:Lang COL_TIME}" Binding="{Binding Timestamp, StringFormat='HH:mm:ss'}" Width="80"/>
                <DataGridTextColumn Header="{ext:Lang COL_LEVEL}" Binding="{Binding Level}" Width="60" FontWeight="Bold"/>
                <DataGridTextColumn Header="{ext:Lang COL_MESSAGE}" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>