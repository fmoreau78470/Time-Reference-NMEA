<!-- Création du fichier : d:\Francis\Documents\code\Time reference NMEA\CSharp_Version\TimeReference.App\ClockVarWindow.xaml -->
<Window x:Class="TimeReference.App.ClockVarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Moniteur NTP (clockvar)" Height="400" Width="600" WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="État du Pilote NMEA (Mise à jour : 1s)" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

        <!-- Tableau de bord -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,0,0,15" Background="#FAFAFA">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Fudge Time 2 (ms) :" FontWeight="Bold"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="LblFudge" Text="--" Foreground="Blue"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Stratum :" FontWeight="Bold" Margin="0,5,0,0"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="LblStratum" Text="--" Margin="0,5,0,0"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Ref ID :" FontWeight="Bold" Margin="0,5,0,0"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="LblRefId" Text="--" Margin="0,5,0,0"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Santé (Poll/NoReply) :" FontWeight="Bold" Margin="0,5,0,0"/>
                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="LblHealth" Text="--" Margin="0,5,0,0"/>
            </Grid>
        </Border>

        <!-- Données brutes -->
        <GroupBox Grid.Row="2" Header="Réponse brute ntpq">
            <TextBox x:Name="TxtRaw" TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas"/>
        </GroupBox>
    </Grid>
</Window>
